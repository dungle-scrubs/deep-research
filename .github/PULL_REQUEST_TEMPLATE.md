## What changes

One concern per PR. Name the pipeline step or module.

## Evidence

- [ ] `pnpm typecheck` clean
- [ ] `pnpm lint` clean
- [ ] `pnpm test` green (note new tests: )
- [ ] `pnpm smoke` exits 0 (if the pipeline surface changed)

If this changes pipeline behavior: which RFC section, and does the spec
need an update in the same PR?
